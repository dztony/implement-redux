import { createStore } from "@/myRedux/createStore";
import {
  IStore,
  IListener,
  IState,
  IAction,
  IReducer,
} from '@/myRedux/types';

export {
  createStore,
}

export type {
  IStore,
  IListener,
  IState,
  IAction,
  IReducer,
}
